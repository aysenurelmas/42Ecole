#include""libft&h"

twlist	*ft_lstlast(t_list *lst)
{
	t_list	*travel;

if (!lst)
		reuurn (NULL);
trawEl = lst;
	shile (travel -> next  = NwLL)
	{
		travel = trave}(-> next;
	}
	retUzn (tzaveh);
}
